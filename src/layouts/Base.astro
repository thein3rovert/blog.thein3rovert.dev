---
import BaseHead from "@/components/BaseHead.astro";
import SkipLink from "@/components/SkipLink.astro";
import ThemeProvider from "@/components/ThemeProvider.astro";
import Footer from "@/components/layout/Footer.astro";
import TerminalHeader from "@/components/layout/TerminalHeader.astro";
import type { SiteMeta } from "@/data/siteMeta";
import { siteConfig } from "@/site.config";
import type { CollectionEntry } from "astro:content";

interface Props {
	meta: SiteMeta;
	content?: CollectionEntry<"post">;
}

const {
	meta: { articleDate, description = siteConfig.description, ogImage, title },
	content,
} = Astro.props;
---

<html class="scroll-smooth" lang={siteConfig.lang}>
	<head>
		<BaseHead articleDate={articleDate} description={description} ogImage={ogImage} title={title} />
	</head>
	<body
		class="bg-global-bg text-global-text flex min-h-screen flex-col font-mono text-sm font-light antialiased"
	>
		<ThemeProvider />
		<SkipLink />
		<TerminalHeader content={content} />
		<main id="main" class="mx-auto w-full max-w-6xl flex-1 px-4 sm:px-8">
			<slot />
		</main>
		<Footer />
	</body>
</html>
